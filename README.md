# catdir
A robust directory-to-context text aggregator.  catdir crawls a directory, filters out binary files, respects depths/sizes,  and outputs a cleanly structured layout or JSON stream. Ideal for gathering  codebase context for analysis or documentation.
